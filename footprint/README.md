# 3D 封装命名规则

## 文件名

1. 使用 BOM 的“封装”字段作为文件名，不使用位号或元件参数。
2. 文件格式统一使用自包含的 `.glb`。
3. 空格可改为下划线；Windows 文件名不支持的字符可删除或改为下划线。
4. 匹配时忽略大小写、空格、下划线、横线和标点。
5. 保留封装中的尺寸、引脚数及方向后缀，例如 `4x4`、`24`、`L`、`N`、`EP`。
6. 不在文件名后附加版本号、颜色或厂商备注。确实属于不同外形时，应在 BOM 封装名中区分。
7. 可以按 `passive`、`ic`、`connector`、`mechanical` 建立子目录，匹配时只读取文件名。
8. `.step` 或 `.stp` 可作为源模型保留，但网页实际加载需要同名的 `.glb`。

例如：

- BOM `C 0603_L` 对应 `C_0603_L.glb`
- BOM `QFN24 4x4_N` 对应 `QFN24_4x4_N.glb`
- BOM `DFN-8-3*3-EP` 对应 `DFN-8-3_3-EP.glb`
- BOM `CM D(6.3*5.4)` 对应 `CM_D_6.3_5.4.glb`

## 模型坐标

- 模型保持真实尺寸，推荐按 glTF 标准使用米作为单位；加载时程序统一换算为毫米。
- 原点 `(0, 0, 0)` 应为封装的贴装原点。
- `Z=0` 应为元件与 PCB 接触的贴装平面，元件本体位于 `+Z`。
- 顶视图中 `+X` 向右、`+Y` 向上，模型的 `0°` 必须与 Altium 封装的 `0°` 一致。
- IC、二极管、电解电容等有方向器件必须保留 1 脚或极性标记。
- 顶层和底层共用同一个模型，程序根据坐标文件自动处理板面和旋转。

## 当前 HexaCharger BOM

### 电阻、电容、保险丝和 LED

- `C_0402_L.glb`
- `C_0603_L.glb`
- `C_0805_L.glb`
- `C_1206_L.glb`
- `R_0402_L.glb`
- `R_0805_L.glb`
- `R_1206_L.glb`
- `R_2512_L.glb`
- `FU_1206.glb`
- `LED_0402G.glb`
- `LED_0402R.glb`
- `CM_D_6.3_5.4.glb`

### IC、晶体管和二极管

- `DFN-8-3_3-EP.glb`
- `LQFP64_N.glb`
- `QFN20_3x3_EP.glb`
- `QFN24_4x4_N.glb`
- `QFN32_4X4.glb`
- `SC70-6.glb`
- `SO-08.glb`
- `SOD-123F.glb`
- `SOD-923.glb`
- `SOT-723.glb`
- `SOT23-3L.glb`
- `SOT23-3N.glb`
- `SOT23-5L.glb`
- `SOT23-6.glb`

### 电感、晶振、蜂鸣器和开关

- `FXL0530-L.glb`
- `HC0420.glb`
- `SMD_13x13.glb`
- `OSC_3225-4P.glb`
- `MLT-8530.glb`
- `TSW_SMD-6_6_4.3.glb`

### 接插件和结构件

- `BT2.0-M.glb`
- `FPC0.5_2H-WS-12P.glb`
- `GCT-USB4105-XX-A_V.glb`
- `HDR1.27-LI-4P.glb`
- `JST-SM02B-SRSS-TB_V.glb`
- `PH2.0-LI-2P_1.6mm.glb`
- `M2_4.0x6.0_2.5x1.2.glb`

`ICO1` 是 Logo，`TEST 0.8` 是测试点，通常不需要实体 3D 模型。

模型目录由本机服务动态扫描。通过页面导入模型后会自动刷新选择器；手工复制模型后刷新页面或重新打开选择窗口即可读取新文件。

## 本地完整模型库

完整 KiCad STEP 库保存在 `footprint/3dmodels`，由于体积较大，该目录不纳入 Git 管理。
分类目录采用中文名称并保留 `.3dshapes` 后缀；原始英文目录名与中文目录名的对应关系记录在
`footprint/3dmodels-folder-map.csv`。STEP 文件名保持不变，自动匹配仍以文件名为主。

### 目录结构

库根目录 `footprint/3dmodels/`，固定两级，目录名即稳定契约：

```
footprint/3dmodels/<中文分类>.3dshapes/<模型>.step
```

- 当前规模：**105 个分类目录 / 7238 个 `.step` / 约 3210.8 MB**（2026-09-19 实测）。
- 后缀统一为 `.step`，没有 `.glb`；`3dmodels/` 下**不得出现包装目录**（如 `3dmodels/3dmodels/…`），也不得放散装模型文件。
- 该目录不入 Git，新检出后需用压缩包铺回（见「从压缩包恢复完整模型库」）。

### 分类目录清单

分类名即目录名去掉 `.3dshapes` 后缀；括号内为该分类的模型数。

**无源元件（19 个分类 / 1624 个模型）**

`保险丝`(28)、`压敏电阻`(100)、`变压器_贴片`(1)、`变压器_通孔`(30)、`振荡器`(9)、`晶体与谐振器`(113)、`滤波器`(8)、`电位器_贴片`(2)、`电位器_通孔`(24)、`电容_贴片`(91)、`电容_通孔`(375)、`电感_贴片`(517)、`电感_贴片_伍尔特Wuerth`(4)、`电感_通孔`(144)、`电感_通孔_伍尔特Wuerth`(10)、`电阻_贴片`(40)、`电阻_通孔`(105)、`钽电容_贴片`(22)、`铁氧体磁芯_通孔`(1)

**半导体与光电（20 个分类 / 1320 个模型）**

`LED_贴片`(38)、`LED_通孔`(87)、`二极管_贴片`(38)、`二极管_通孔`(93)、`光电器件`(42)、`晶体管_功率模块`(3)、`电子管`(1)、`芯片封装_BGA`(69)、`芯片封装_CSP`(34)、`芯片封装_DFN-QFN`(187)、`芯片封装_DIP`(204)、`芯片封装_DirectFET`(13)、`芯片封装_LCC`(1)、`芯片封装_LGA`(22)、`芯片封装_QFP`(62)、`芯片封装_SIP`(11)、`芯片封装_SO`(210)、`芯片封装_SON`(34)、`芯片封装_TO-SOT_贴片`(78)、`芯片封装_TO-SOT_通孔`(93)

**连接器与端子（39 个分类 / 3497 个模型）**

`排母_1.00mm`(156)、`排母_1.27mm`(246)、`排母_2.00mm`(278)、`排母_2.54mm`(278)、`排针_1.00mm`(278)、`排针_1.27mm`(278)、`排针_2.00mm`(278)、`排针_2.54mm`(278)、`接线端子_Altech`(23)、`接线端子_伍尔特Wuerth`(13)、`接线端子_宁波Kagnex`(22)、`接线端子_菲尼克斯`(98)、`连接器_D-Sub`(96)、`连接器_DC圆孔电源`(5)、`连接器_DIN`(10)、`连接器_FFC-FPC`(112)、`连接器_IDC`(51)、`连接器_JST`(158)、`连接器_RJ`(3)、`连接器_SATA-SAS`(1)、`连接器_USB`(12)、`连接器_伍尔特Wuerth`(37)、`连接器_卡座`(3)、`连接器_同轴射频`(14)、`连接器_插针`(11)、`连接器_斯托科Stocko`(19)、`连接器_泰科TE`(2)、`连接器_申泰Samtec`(1)、`连接器_申泰Samtec_HPM通孔`(40)、`连接器_艾迈斯AMASS`(2)、`连接器_莫仕Molex`(89)、`连接器_菲尼克斯_GMSTB`(88)、`连接器_菲尼克斯_MC`(180)、`连接器_菲尼克斯_MC高压`(66)、`连接器_菲尼克斯_MSTB`(180)、`连接器_菲尼克斯_SPT`(69)、`连接器_视频`(9)、`连接器_通用`(9)、`连接器_音频`(4)

**机电与电源（8 个分类 / 440 个模型）**

`按键开关_贴片`(157)、`按键开关_通孔`(81)、`电池`(29)、`电源转换器_AC-DC`(24)、`电源转换器_DC-DC`(52)、`继电器_贴片`(17)、`继电器_通孔`(64)、`蜂鸣器`(16)

**传感器与射频（14 个分类 / 126 个模型）**

`传感器_压力`(1)、`传感器_湿度`(1)、`传感器_电压`(1)、`传感器_电流`(4)、`传感器_距离`(1)、`传感器_通用`(1)、`传感器_音频`(5)、`射频_GSM`(1)、`射频_天线`(4)、`射频_模块`(18)、`射频_转换器`(3)、`数码管_七段`(44)、`显示器件_通用`(32)、`模块`(10)

**结构与安装件（5 个分类 / 231 个模型）**

`安装件_伍尔特Wuerth`(191)、`安装件_通用`(1)、`散热器`(14)、`标识符号`(2)、`测试点`(23)

### 维护规则（导入前必读）

三处登记必须保持一致（2026-09-19 实测 105 / 105 / 105，双向零差异）：

| 位置 | 作用 |
| --- | --- |
| `footprint/3dmodels/*.3dshapes/` | 磁盘上的真实目录，导入落盘的目标 |
| `src/footprint-categories.ts` 的 `folderCategories` | 105 个目录 → 6 个大类的归属与中文显示名；**未登记的目录会回落到「其他」大类** |
| `footprint/3dmodels-folder-map.csv` | `OriginalName`（英文）→ `ChineseName`（中文目录名）映射 |

- 后端 `footprint_category_dir()` 要求 `footprint/3dmodels/<分类>` **已存在且是一级目录**；`archive_category_lookup()` 也只扫描一级目录。因此**多一层包装目录会让每次导入都返回「分类目录不存在：<分类>」**，并让压缩包归位失效（转而新建重复的顶层分类目录）。
- 手工解压时不要把压缩包里的包装目录一起拷进 `footprint/3dmodels/`（正确结果是 `3dmodels/<分类>.3dshapes/…`，不是 `3dmodels/<包装目录>/<分类>.3dshapes/…`）。
- 网页端导入（`POST /api/footprint/import`）按包内**最近的** `<分类>.3dshapes/` 归一，不会产生包装层；命中库内已有分类则直接归位，库内没有的分类名会自动新建（新目录未写入 `src/footprint-categories.ts`，会先落在「其他」大类）。
- 库为空时，面板会给出「按包内分类自动归位」档位（`category=__auto__`，前后端常量 `AUTO_CATEGORY_KEY` / `footprintAutoCategory`）：不指定默认分类，完全按包内结构重建分类目录，没有 `<分类>.3dshapes/` 层级的条目被跳过。因此网页端也能把整库从零恢复，不需要先留一个分类目录；单个模型文件仍必须有已存在的分类目录。
- 新增分类目录后，模型服务会自动列出该目录；同时在 `src/footprint-categories.ts` 与 CSV 中登记，才能让它归入六个业务大类并保留原始英文目录映射。未登记的目录会先显示在「其他」。

## 从 KiCad 10 同步

项目使用 `scripts/sync-kicad-footprints.ps1` 从本机 KiCad 模型库复制当前 BOM 所需的标准 STEP，并生成 `library-manifest.json`。脚本不会删除已有模型。

```powershell
& .\scripts\sync-kicad-footprints.ps1
```

生成的清单会区分：

- `exact`：封装系列和主要尺寸一致。
- `user`：优先采用用户提供的模型。
- `compatible`：外形接近，正式生产前需要确认尺寸和贴装原点。
- `manual-needed`：没有可靠的 KiCad 模型，不会自动使用替代品。

## 从压缩包恢复完整模型库

完整模型库（105 个中文分类目录 / 7238 个 `.step` / 约 3.2 GB）不纳入 Git，换机器或清理磁盘后需要用压缩包铺回。
压缩包内部结构约定为：

```
<任意包装目录>/<中文分类>.3dshapes/<模型>.step
```

`scripts/extract-3dmodels-archive.py` 会剥离包装目录，落盘为应用契约要求的
`footprint/3dmodels/<中文分类>.3dshapes/<模型>.step`。脚本具备 zip-slip 防护、后缀白名单、
重复目标检测、原子写入（临时文件 + `os.replace`）与幂等重跑（已存在同名文件默认跳过，加 `--overwrite` 覆盖）。

```powershell
# 干跑：只读校验分类数、文件数与映射表一致性，不写盘
npm run models:import -- "C:\path\to\3dmodels.zip" --check-folder-map footprint/3dmodels-folder-map.csv

# 正式铺库
npm run models:import -- "C:\path\to\3dmodels.zip" --write
```

完整模型库由本机模型服务动态读取，Vite 不监听 `footprint/3dmodels`。铺库期间可保持开发服务器运行；铺完刷新页面或重新打开 STEP 选择窗口即可读取新模型。

## 转换为网页模型

同步 STEP 后运行以下命令，使用本机 FreeCAD 将清单中的模型批量转换为同名 GLB：

```powershell
npm run models:convert
```

转换器只更新比 STEP 旧的 GLB；需要全部重建时运行：

```powershell
& .\scripts\convert-step-models.ps1 -Force
```

网页会按 BOM 的封装字段自动查找同名 GLB，并根据坐标文件中的位号、板面和旋转角度放置模型。未找到模型或加载失败的元件继续显示尺寸占位块。
